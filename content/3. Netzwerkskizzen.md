
# Netzwerkskizzen

```dataviewjs
const pdfFiles = app.vault.getFiles() .filter(file => file.extension === 'md' && file.path.includes('netzwerkskizzen')) .sort((a, b) => a.basename.localeCompare(b.basename)); 

dv.list(pdfFiles.map(file => dv.fileLink(file.path)));
```
